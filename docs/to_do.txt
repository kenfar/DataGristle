Very next things to do:
6.  Add more numeric analysis:  std, variance, quartiles
8.  test with python 2.7
9.  Review documentation
11. Review dist utils
12. Test on mac


General Things To Do:

1.  determine:
    - field types                               - 90%
    - field min, max, mean, median values       - 95%
    - field length min, max, mean, median	- 50%
    - field names                               - 100%
    - field case                                - 100%
    - field histogram                           - 100%
    - format consistency
    - field 
    - field correlations
    - file web page?
    - find invalid characters
    - find inconsistent code sets
    - find required vs optional fields
    - shallow determinator vs deep-determinator

2.  metadata
    - allow use of yaml (decentralized) or central repository
    - generate metadata

3.  what it isn't
    - enterprise ETL tool

4.  Add other data formats to analyze:
    - relational databases
    - files (fixed length records, xml, yaml, json, html)

5.  misc
    - setup tools
    - testing for various versions of python
    - testing on various OS

6.  histogrammer
    - automatic bucketing:
      - integers:    
        - min & max range calculation - then determine buckets from that
      - dates & times
        - min & max range calculation
      - strings
        - sort file - take most common?
        - change to all lower case?
        - truncate?
        - hash?
    
